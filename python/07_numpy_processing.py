import numpy as np

# Create an array
arr = np.array([1, 2, 3, 4, 5])

# Perform operations
arr_squared = np.square(arr)
arr_sum = np.sum(arr)

print(f'Squared: {arr_squared}')
print(f'Sum: {arr_sum}')
